webdriver {
  driver = "chrome"
  autodownload = true
  base.url = "http://localhost:3000"
}

api {
  crud {
    base.url = "http://localhost:8002"
  }
}

headless.mode = false

serenity {
  project.name = "AUTO_FRONT_SCREENPLAY"
  test.root = "runners"
}
